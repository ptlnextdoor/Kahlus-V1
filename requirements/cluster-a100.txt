# Install CUDA-enabled PyTorch first using the wheel index for the Chapman CUDA version.
# Example for CUDA 12.1:
#   python -m pip install --index-url https://download.pytorch.org/whl/cu121 torch torchvision torchaudio
#
# Then install the project and cluster extras:
#   python -m pip install -e '.[moabb,cluster]'

moabb>=1.1
mne>=1.6
mne-bids>=0.14
PyYAML>=6.0
numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
tensorboard>=2.16
wandb>=0.16
