# License And Reuse

Permissive upstreams may be pinned and vendored after license inspection.

Mixed or restricted upstreams stay behind adapters and cannot be required for open reproduction. BrainLM and SpikingJelly are quarantined unless license review permits a narrower use.

Use:

```bash
python scripts/vendor_upstreams.py --dry-run
```
